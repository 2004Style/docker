services:
  mysql:
    image: mysql:latest
    container_name: mysql-server
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: pruebas
      MYSQL_USER: rdev
      MYSQL_PASSWORD: rdev
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
